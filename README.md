# homebrew-tracebar

Homebrew tap for [TraceBar](https://github.com/evilscheme/tracebar) — continuous graphical traceroute monitoring in your macOS menubar.

## Install

```bash
brew tap evilscheme/tracebar
brew install --cask tracebar
```

## Update

```bash
brew upgrade --cask tracebar
```
